## Futter Future Builder Example
this Example how to to fetch api with model in flutter with future builder

## List i must work
- GET (READ) :heavy_check_mark:
- POST (CREATE) :x:
- PUT (UPDATE) :x:
- DELETE (DELETE) :x:
